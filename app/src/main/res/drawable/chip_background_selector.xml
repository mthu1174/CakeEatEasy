<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Trạng thái khi Chip được chọn (checked) -->
    <item android:color="@color/app_pink" android:state_checked="true"/>
    <!-- Trạng thái mặc định (không được chọn) -->
    <item android:color="@color/app_pink_background"/>
</selector>