<?xml version="1.0" encoding="utf-8"?>
<!--
    SỬA: Bỏ LinearLayout ngoài cùng và dùng CardView làm gốc.
    Việc này giúp giảm một tầng layout không cần thiết (tối ưu hiệu năng).
-->
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:cardCornerRadius="16dp"
    app:cardElevation="2dp"
    android:layout_marginHorizontal="16dp"
    android:layout_marginVertical="8dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- SỬA: Gộp thông tin người dùng và rating lên trên cùng -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:layout_marginBottom="8dp">

            <ImageView
                android:id="@+id/img_review_avatar"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:scaleType="centerCrop"
                tools:src="@drawable/ic_avatar" />

            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="12dp"
                android:layout_weight="1"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/txt_review_user_name"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:fontFamily="@font/bold"
                    android:textColor="@color/text_dark"
                    android:textSize="16sp"
                    android:textStyle="bold"
                    tools:text="Ngoc Le" />

                <!-- SỬA: Dùng RatingBar thay vì 5 ImageView -->
                <RatingBar
                    android:id="@+id/rating_bar_review"
                    style="?android:attr/ratingBarStyleSmall"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:isIndicator="true"
                    android:numStars="5"
                    android:stepSize="0.5"
                    android:theme="@style/RatingBarTheme"
                    tools:rating="5" />
            </LinearLayout>

            <TextView
                android:id="@+id/txt_review_date"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@color/text_light"
                android:textSize="12sp"
                tools:text="17/05/2024"/>
        </LinearLayout>

        <!-- SỬA: Đã bỏ Review Title vì thường không cần thiết, chỉ cần nội dung comment -->
        <!-- Review Content -->
        <TextView
            android:id="@+id/txt_review_comment"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="@font/regular"
            android:textColor="@color/text_normal"
            android:textSize="14sp"
            tools:text="I ordered this cake for my mom's birthday, and it was absolutely perfect. The size was just right for our family of 8, and everyone loved the flavor — not too sweet, very balanced." />

    </LinearLayout>
</androidx.cardview.widget.CardView>